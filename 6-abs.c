#include "main.h"

/**
 *_abs - computes the absoulute value.
 *
 *@i: integer input.
 *
 *Return: return absolute value.
 */

int _abs(int i)
{
	if (i > 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
